services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules_data:/app/node_modules
    environment:
      - NODE_ENV=development

volumes:
  node_modules_data:
