services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules_data:/app/node_modules
    env_file:
      - .env.local
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:8080
    command: sh -c "bun install --frozen-lockfile && bun --bun run dev"

volumes:
  node_modules_data:
