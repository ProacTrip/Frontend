//interfaz por si se quieren crear nuevas cartas y s quiere modificar lo q vaya a tener cada una dentro
export interface Destination {
  id: number;
  name: string;
  place: string;
  description: string;
  image: string;
}